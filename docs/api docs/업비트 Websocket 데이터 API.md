# 업비트 Websocket 통신 API 문서 

## 채결 

해당 API 문서는 **업비트(Upbit) 거래소의 웹소켓(WebSocket)을 통해 실시간 체결 데이터를 구독**하는 방법에 대한 명세입니다. 문서는 크게 '요청 메시지'와 '구독 데이터' 두 부분으로 나뉩니다.

### **요청 메시지 형식**

웹소켓 연결 후, 실시간 체결 데이터를 받기 위해 보내야 하는 요청 메시지의 형식을 정의합니다.

  - **주요 필드**

      - `type` (필수): 데이터 종류를 나타내며, 체결 데이터를 받으려면 반드시 \*\*`trade`\*\*로 설정해야 합니다.
      - `codes` (필수): 수신하고자 하는 가상자산 페어 목록을 리스트 형태로 지정합니다. **반드시 대문자**로 보내야 합니다 (예: `["KRW-BTC", "KRW-ETH"]`).
      - `is_only_snapshot` (선택): `true`로 설정 시, 현재 시점의 스냅샷 데이터만 한 번 받고 실시간 스트림은 받지 않습니다. 기본값은 `false`입니다.
      - `is_only_realtime` (선택): `true`로 설정 시, 스냅샷 데이터 없이 실시간 체결 데이터만 받습니다. 기본값은 `false`입니다.

  - **요청 예시**
    요청 시에는 `ticket`(고유 식별자), `type` 객체, 그리고 `format`(데이터 형식)을 배열에 담아 JSON 형식으로 전송합니다.

    ```json
    [
      { "ticket": "고유한 티켓값" },
      {
        "type": "trade",
        "codes": ["KRW-BTC", "KRW-ETH"]
      },
      { "format": "DEFAULT" }
    ]
    ```

-----

### **구독 데이터 명세**

요청이 성공하면 웹소켓을 통해 받게 되는 실시간 체결 데이터의 상세 항목을 설명합니다.

  - **주요 데이터 필드**

      - `type` (`ty`): 데이터 항목 (`trade`)
      - `code` (`cd`): 페어 코드 (예: `KRW-BTC`)
      - `trade_price` (`tp`): 체결 가격
      - `trade_volume` (`tv`): 체결량
      - `ask_bid` (`ab`): 매수/매도 구분 (`ASK`: 매도, `BID`: 매수)
      - `prev_closing_price` (`pcp`): 전일 종가
      - `change` (`c`): 전일 대비 등락 상태 (`RISE`: 상승, `EVEN`: 보합, `FALL`: 하락)
      - `change_price` (`cp`): 전일 대비 변동 가격의 절대값
      - `trade_timestamp` (`ttms`): 체결 시각 타임스탬프 (밀리초 단위)
      - `stream_type` (`st`): 스트림 타입 (`SNAPSHOT`: 스냅샷, `REALTIME`: 실시간)
      - `best_ask_price` (`bap`): 최우선 매도 호가
      - `best_bid_price` (`bbp`): 최우선 매수 호가

  - **데이터 예시**
    아래는 KRW-BTC 페어의 스냅샷 체결 데이터 예시입니다.

    ```json
    {
      "type": "trade",
      "code": "KRW-BTC",
      "timestamp": 1730336862082,
      "trade_date": "2024-10-31",
      "trade_time": "01:07:42",
      "trade_timestamp": 1730336862047,
      "trade_price": 100473000.0,
      "trade_volume": 0.00014208,
      "ask_bid": "BID",
      "prev_closing_price": 100571000.0,
      "change": "FALL",
      "change_price": 98000.0,
      "sequential_id": 17303368620470000,
      "stream_type": "SNAPSHOT"
    }
    ```
--- 

## 오더북 

해당 문서는 **업비트(Upbit) 웹소켓(WebSocket) API를 통해 실시간 호가(오더북) 정보를 구독**하는 방법을 설명합니다. 요청 시 특정 단위로 호가를 그룹화하거나 조회할 호가 개수를 지정하는 옵션이 있습니다.

### **요청 메시지 형식**

웹소켓으로 호가 정보를 받기 위해 보내는 요청 메시지의 구조와 주요 옵션을 정의합니다.

  - **핵심 필드**

      - `type` (필수): 반드시 \*\*`orderbook`\*\*으로 지정해야 합니다.
      - `codes` (필수): 구독할 페어(마켓) 목록을 리스트 형태로 지정합니다. **반드시 대문자**여야 합니다. (예: `["KRW-BTC"]`)

  - **주요 옵션**

    1.  **호가 모아보기 (`level`)**: **원화(KRW) 마켓 전용 기능**으로, 지정된 가격 단위로 호가를 묶어서 보여줍니다. 예를 들어, `level`을 `10000`으로 설정하면 1만 원 단위로 매수/매도 호가를 그룹화하고, 해당 가격 범위 내의 주문량을 합산하여 보여줍니다.
          - **주의**: 마켓별로 지원하는 `level` 단위가 다르므로, 사용 전 반드시 확인해야 합니다.
    2.  **호가 조회 개수 지정 (`unit`)**: 조회할 호가 쌍(매수/매도)의 개수를 지정할 수 있습니다. 페어 코드 뒤에 마침표(`.`)와 숫자를 붙여 요청합니다.
          - **형식**: `{페어 코드}.{조회 단위}` (예: `KRW-ETH.5`는 ETH/KRW 마켓의 호가를 5개씩 조회)
          - **지원 단위**: `1`, `5`, `15`, `30` (기본값은 `30`)

  - **요청 예시**
    KRW-BTC는 10,000원 단위로 호가를 모아보고, KRW-ETH는 5개의 호가만 조회하는 요청입니다.

    ```json
    [
      { "ticket": "고유한 티켓값" },
      {
        "type": "orderbook",
        "codes": ["KRW-BTC", "KRW-ETH.5"],
        "level": 10000
      },
      { "format": "DEFAULT" }
    ]
    ```

-----

### **구독 데이터 명세**

요청이 성공하면 웹소켓을 통해 받게 되는 실시간 호가 데이터의 상세 항목입니다.

  - **주요 데이터 필드**

      - `type` (`ty`): 데이터 타입 (`orderbook`)
      - `code` (`cd`): 마켓 코드 (예: `KRW-BTC`)
      - `total_ask_size` (`tas`): 해당 마켓의 **매도 주문 총량**
      - `total_bid_size` (`tbs`): 해당 마켓의 **매수 주문 총량**
      - `orderbook_units` (`obu`): 실제 매수/매도 호가 정보가 담긴 리스트
          - `ask_price` (`ap`): 매도 호가
          - `bid_price` (`bp`): 매수 호가
          - `ask_size` (`as`): 해당 매도 호가의 주문량
          - `bid_size` (`bs`): 해당 매수 호가의 주문량
      - `level` (`lv`): 요청 시 적용된 호가 모아보기 단위
      - `stream_type` (`st`): 데이터 종류 (`SNAPSHOT`: 최초 스냅샷, `REALTIME`: 실시간 변동)

  - **데이터 예시**
    KRW-BTC 마켓의 실시간 호가 정보입니다. `orderbook_units` 배열 안에 각 가격대의 매수/매도 호가와 주문량이 포함되어 있습니다.

    ```json
    {
      "type": "orderbook",
      "code": "KRW-BTC",
      "total_ask_size": 4.79158413,
      "total_bid_size": 2.65609625,
      "orderbook_units": [
        {
          "ask_price": 137002000,
          "bid_price": 137001000,
          "ask_size": 0.10623869,
          "bid_size": 0.03656812
        },
        {
          "ask_price": 137023000,
          "bid_price": 137000000,
          "ask_size": 0.06144079,
          "bid_size": 0.33543284
        }
        // ... (이하 생략)
      ],
      "stream_type": "SNAPSHOT",
      "level": 0
    }
    ```

---

## 현재가(ticket) 

해당 문서는 **업비트(Upbit) 웹소켓(WebSocket) API를 통해 특정 가상자산의 현재가 정보를 실시간으로 구독**하는 방법을 설명합니다. '현재가'는 시세, 체결, 호가 정보를 종합한 데이터로, '티커(Ticker)'라고도 불립니다.

### **요청 메시지 형식**

실시간 현재가(Ticker) 데이터를 받기 위해 웹소켓 연결 후 전송해야 하는 요청 메시지의 구조입니다.

  * **주요 필드**

      * `type` (필수): 데이터 종류를 나타내며, 현재가 정보를 받으려면 반드시 \*\*`ticker`\*\*로 설정해야 합니다.
      * `codes` (필수): 수신하고자 하는 가상자산 페어 목록을 리스트 형태로 지정합니다. **반드시 대문자**로 요청해야 합니다 (예: `["KRW-BTC", "KRW-ETH"]`).
      * `is_only_snapshot` (선택): `true`로 설정하면 현재 시점의 스냅샷 데이터만 받고 실시간 스트림은 받지 않습니다. 기본값은 `false`입니다.
      * `is_only_realtime` (선택): `true`로 설정하면 스냅샷 데이터 없이 실시간 데이터만 받습니다. 기본값은 `false`입니다.

  * **요청 예시**
    요청 시 `ticket`(고유 식별자), `type` 객체, 그리고 `format`(데이터 형식)을 배열에 담아 JSON 형식으로 전송합니다.

    ```json
    [
      { "ticket": "고유한 티켓값" },
      {
        "type": "ticker",
        "codes": ["KRW-BTC", "KRW-ETH"]
      },
      { "format": "DEFAULT" }
    ]
    ```

-----

### **구독 데이터 명세**

요청이 성공하면 웹소켓을 통해 받게 되는 실시간 현재가 데이터의 상세 항목입니다.

  * **주요 데이터 필드**

      * `type` (`ty`): 데이터 항목 (`ticker`)
      * `code` (`cd`): 페어 코드 (예: `KRW-BTC`)
      * `opening_price` (`op`): 시가 (UTC 0시 기준)
      * `high_price` (`hp`): 고가 (UTC 0시 기준)
      * `low_price` (`lp`): 저가 (UTC 0시 기준)
      * `trade_price` (`tp`): **현재가** (가장 최근 체결가)
      * `prev_closing_price` (`pcp`): 전일 종가
      * `change` (`c`): 전일 대비 등락 상태 (`RISE`: 상승, `EVEN`: 보합, `FALL`: 하락)
      * `change_price` (`cp`): 전일 대비 변동액
      * `change_rate` (`cr`): 전일 대비 등락률
      * `acc_trade_volume_24h` (`atv24h`): **24시간 누적 거래량**
      * `acc_trade_price_24h` (`atp24h`): **24시간 누적 거래대금**
      * `highest_52_week_price` (`h52wp`): 52주 신고가
      * `lowest_52_week_price` (`l52wp`): 52주 신저가
      * `market_state` (`ms`): 거래 상태 (`ACTIVE`: 거래 가능, `DELISTED`: 상장 폐지 등)
      * `market_warning` (`mw`): 유의 종목 지정 여부 (`NONE`, `CAUTION`)
      * `stream_type` (`st`): 데이터 종류 (`SNAPSHOT`: 스냅샷, `REALTIME`: 실시간)

  * **데이터 예시**
    아래는 KRW-BTC 페어의 실시간 현재가 데이터 예시입니다.

    ```json
    {
      "type": "ticker",
      "code": "KRW-BTC",
      "opening_price": 31883000,
      "high_price": 32310000,
      "low_price": 31855000,
      "trade_price": 32287000,
      "prev_closing_price": 31883000.0,
      "change": "RISE",
      "change_price": 404000.0,
      "change_rate": 0.0126713295,
      "acc_trade_volume_24h": 7158.8028356,
      "acc_trade_price_24h": 228827082483.70729,
      "stream_type": "REALTIME"
    }
    ```

--- 
## 캔들 

이 문서는 **업비트(Upbit) 웹소켓(WebSocket) API를 통해 실시간 캔들(Candlestick) 데이터를 구독**하는 방법을 설명합니다.

### **주요 특징 (데이터 전송 방식)**

  - **데이터 생성 조건**: 캔들 데이터는 해당 시간 단위에 **실제 체결이 발생했을 때만 생성되어 전송**됩니다. 만약 1분 봉을 구독 중인데 1분 동안 거래가 없다면 해당 1분 봉 데이터는 전송되지 않습니다.
  - **데이터 전송 주기**: 데이터는 **1초 간격**으로 전송될 수 있습니다.
  - **데이터 업데이트**: 하나의 캔들(예: 오후 2시 15분의 5분 봉)은 해당 시간 동안 **여러 번 전송될 수 있습니다.** 새로운 체결이 발생할 때마다 해당 캔들의 고가, 저가, 종가, 거래량이 갱신되어 다시 전송됩니다. 따라서 항상 **가장 마지막에 받은 데이터가 최신 정보**입니다.

-----

### **요청 메시지 형식**

실시간 캔들 데이터를 받기 위해 웹소켓으로 보내는 요청 메시지의 구조입니다.

  - **핵심 필드**

      - **`type`** (필수): 구독할 캔들의 종류를 지정합니다.
          - `candle.1s`: 초봉
          - `candle.1m`: 1분봉
          - `candle.3m`: 3분봉
          - `candle.5m`: 5분봉
          - `candle.10m`: 10분봉
          - `candle.15m`: 15분봉
          - `candle.30m`: 30분봉
          - `candle.60m`: 60분봉
          - `candle.240m`: 240분봉
      - **`codes`** (필수): 수신할 페어(마켓) 목록을 **대문자**로 지정합니다. (예: `["KRW-BTC", "KRW-ETH"]`)

  - **요청 예시**
    KRW-BTC와 KRW-ETH 마켓의 **1분봉** 데이터를 요청하는 예시입니다.

    ```json
    [
      { "ticket": "고유한 티켓값" },
      {
        "type": "candle.1m",
        "codes": ["KRW-BTC", "KRW-ETH"]
      },
      { "format": "DEFAULT" }
    ]
    ```

-----

### **구독 데이터 명세**

요청이 성공하면 받게 되는 실시간 캔들 데이터의 상세 항목입니다.

  - **주요 데이터 필드**

      - `type` (`ty`): 요청한 캔들 종류 (예: `candle.1m`)
      - `code` (`cd`): 마켓 코드 (예: `KRW-BTC`)
      - `candle_date_time_utc` (`cdttmu`): 캔들 기준 시각 (UTC)
      - `candle_date_time_kst` (`cdttmk`): 캔들 기준 시각 (KST)
      - `opening_price` (`op`): **시가**
      - `high_price` (`hp`): **고가**
      - `low_price` (`lp`): **저가**
      - `trade_price` (`tp`): **종가** (해당 캔들의 마지막 체결가)
      - `candle_acc_trade_volume` (`catv`): 해당 캔들의 **누적 거래량**
      - `candle_acc_trade_price` (`catp`): 해당 캔들의 **누적 거래대금**

  - **데이터 예시**
    KRW-BTC 마켓의 1초봉 실시간 데이터 예시입니다.

    ```json
    {
      "type": "candle.1s",
      "code": "KRW-BTC",
      "candle_date_time_utc": "2025-01-02T04:28:05",
      "candle_date_time_kst": "2025-01-02T13:28:05",
      "opening_price": 142009000.0,
      "high_price": 142009000.0,
      "low_price": 142009000.0,
      "trade_price": 142009000.0,
      "candle_acc_trade_volume": 0.00606119,
      "candle_acc_trade_price": 860743.53071,
      "stream_type": "REALTIME"
    }
    ```

---

## 내 주문 및 채결 

해당 문서는 **업비트(Upbit) 웹소켓(WebSocket) API를 통해 자신의 주문 및 체결 내역을 실시간으로 수신**하는 방법을 설명합니다. 이 기능은 **인증된 사용자만** 사용할 수 있습니다.

### **주요 특징 (데이터 전송 방식)**

  - **이벤트 기반 전송**: 데이터는 **실제 주문 상태 변경(예: 신규 주문 접수, 체결, 취소)이나 체결이 발생했을 때만 전송**됩니다. 아무런 활동이 없으면 데이터가 수신되지 않는 것이 정상입니다.
  - **인증 필수**: 개인의 주문 정보이므로, 웹소켓 연결 시 반드시 API 키를 이용한 **인증 절차**를 거쳐야 합니다.

-----

### **요청 메시지 형식**

자신의 주문 및 체결 내역을 실시간으로 받기 위해 웹소켓으로 보내는 요청 메시지의 구조입니다.

  - **핵심 필드**

      - **`type`** (필수): 반드시 \*\*`myOrder`\*\*로 지정해야 합니다.
      - **`codes`** (선택): 특정 페어(마켓)의 주문/체결 내역만 구독하고 싶을 때 사용합니다. (예: `["KRW-BTC"]`)
          - 이 필드를 **생략하거나 빈 배열(`[]`)로 보내면 구독 중인 모든 마켓**의 주문/체결 내역을 수신합니다.

  - **요청 예시**

      - **모든 마켓**의 주문 및 체결 내역 구독
        ```json
        [
          { "ticket": "고유한 티켓값" },
          { "type": "myOrder" }
        ]
        ```
      - **특정 마켓**(KRW-BTC)의 주문 및 체결 내역만 구독
        ```json
        [
          { "ticket": "고유한 티켓값" },
          {
            "type": "myOrder",
            "codes": ["KRW-BTC"]
          }
        ]
        ```

-----

### **구독 데이터 명세**

요청이 성공하면 주문 상태 변경 또는 체결 발생 시 받게 되는 데이터의 상세 항목입니다.

  - **핵심 데이터 필드**

      - `type` (`ty`): 데이터 타입 (`myOrder`)
      - `code` (`cd`): 마켓 코드 (예: `KRW-BTC`)
      - `uuid` (`uid`): 주문의 고유 식별자
      - `state` (`s`): **주문의 현재 상태**
          - `wait`: 체결 대기 중
          - `trade`: 부분 또는 전체 **체결 발생**
          - `done`: 전체 체결 완료
          - `cancel`: 주문 취소
          - `prevented`: 자전 거래 방지로 인해 체결이 방지됨
      - `order_type` (`ot`): 주문 종류 (`limit`: 지정가, `market`: 시장가 매도 등)
      - `price` (`p`): 주문 가격 (상태가 `trade`일 경우, 해당 체결의 가격)
      - `volume` (`v`): 주문량 (상태가 `trade`일 경우, 해당 체결량)
      - `remaining_volume` (`rv`): 체결 후 남은 주문량
      - `executed_volume` (`ev`): 현재까지 체결된 총 수량
      - `paid_fee` (`pf`): 지불된 수수료
      - `smp_type` (`smpt`): **자전 거래 방지(SMP)** 작동 방식 (`cancel_maker`: 기존 메이커 주문 취소 등)
      - `trade_timestamp` (`ttms`): 체결이 발생한 시각 (밀리초)

  - **데이터 예시**
    KRW-BTC 마켓에서 지정가 매수 주문이 \*\*부분 체결(`state: "trade"`)\*\*되었을 때의 데이터 예시입니다.

    ```json
    {
      "type": "myOrder",
      "code": "KRW-BTC",
      "uuid": "ac2dc2a3-fce9-40a2-a4f6-5987c25c438f",
      "ask_bid": "BID",
      "order_type": "limit",
      "state": "trade",
      "trade_uuid": "68315169-fba4-4175-ade3-aff14a616657",
      "price": 0.001453,
      "volume": 30925891.29839369,
      "remaining_volume": 29968038.09235948,
      "executed_volume": 957853.20603421,
      "paid_fee": 22.467660028283017,
      "trade_timestamp": 1710751590421
    }
    ```

---
## 내 자산

해당 문서는 **업비트(Upbit) 웹소켓(WebSocket) API를 통해 자신의 자산 변동 내역을 실시간으로 수신**하는 방법을 설명합니다. 이 기능은 **인증된 사용자만** 사용할 수 있습니다.

### **주요 특징 (데이터 전송 방식)** 📋

  * **이벤트 기반 전송**: 데이터는 입출금, 주문 체결 등 **실제 자산에 변동이 생겼을 때만 전송**됩니다. 자산 변동이 없으면 데이터가 수신되지 않는 것이 정상입니다.
  * **인증 필수**: 개인의 자산 정보이므로, 웹소켓 연결 시 반드시 API 키를 이용한 **인증 절차**가 필요합니다.
  * **최초 이용 시 주의사항**: 이 스트림을 **계정 생성 후 처음으로 구독하는 경우**, 자산 변동이 발생해도 **수 분간 데이터가 지연될 수 있습니다.** 최초 연결 이후에는 재연결 시 즉시 데이터를 수신할 수 있으므로, 첫 사용 시에는 반드시 데이터 수신을 확인해야 합니다.

-----

### **요청 메시지 형식** ✉️

자산 변동 내역을 실시간으로 받기 위해 웹소켓으로 보내는 요청 메시지의 구조입니다.

  * **핵심 필드**

      * **`type`** (필수): 반드시 \*\*`myAsset`\*\*으로 지정해야 합니다.

  * **중요**: 다른 데이터 타입과 달리, `myAsset` 요청 시에는 페어(마켓) 코드를 지정하는 **`codes` 파라미터를 사용하면 안 됩니다.** 포함 시 오류가 발생합니다.

  * **요청 예시**
    아주 간단하게 `type`만 지정하여 요청합니다.

    ```json
    [
      { "ticket": "고유한 티켓값" },
      { "type": "myAsset" }
    ]
    ```

-----

### **구독 데이터 명세** 📊

요청이 성공하면 자산 변동 발생 시 받게 되는 데이터의 상세 항목입니다.

  * **핵심 데이터 필드**

      * `type` (`ty`): 데이터 타입 (`myAsset`).
      * `assets` (`ast`): **자산 목록**이 담긴 리스트. 변동이 발생한 자산의 정보만 포함될 수 있습니다.
          * `currency` (`cu`): 화폐 코드 (예: `KRW`, `BTC`).
          * `balance` (`b`): **주문 가능한 잔고**.
          * `locked` (`l`): 주문 등으로 인해 **묶여있는 잔고**.

  * **데이터 예시**
    원화(KRW) 자산에 변동이 발생했을 때의 데이터 예시입니다. `balance`는 즉시 사용 가능한 금액, `locked`는 주문에 사용 중인 금액을 의미합니다.

    ```json
    {
      "type": "myAsset",
      "assets": [
        {
          "currency": "KRW",
          "balance": 1386929.3723,
          "locked": 10329.6701
        }
      ],
      "asset_timestamp": 1710146517259
    }
    ```
--- 

## 구독중인 스트림 목록 조회

This API lets you check which real-time data streams you are currently subscribed to on an active WebSocket connection.

-----

### Request Format ✉️

Unlike data subscription requests that use a `"type"` field, this is an operational request and uses a `"method"` field.

  * **Key Field**:

      * **`method`** (Required): You must set this to **`LIST_SUBSCRIPTIONS`**.

  * **Example Request**:
    Simply send a JSON array containing your unique `ticket` and the `method` object.

    ```json
    [
      {
        "ticket": "your-unique-ticket"
      },
      {
        "method": "LIST_SUBSCRIPTIONS"
      }
    ]
    ```

> **⚠️ Important Warning**:
> When you make this request, be careful with the `format` field (e.g., `DEFAULT`, `SIMPLE_LIST`). If the format you specify here is different from the one you used for your original subscriptions, it will **change the format of all your active data streams**.

-----

### Response Specification 📊

The response will provide a list of all your current subscriptions on that WebSocket connection.

  * **Key Fields**:

      * **`method`**: Confirms the request was `LIST_SUBSCRIPTIONS`.
      * **`result`**: A list where each object represents an active subscription.
          * `type`: The type of data stream (e.g., `ticker`, `orderbook`, `myOrder`).
          * `codes`: The list of market codes you are subscribed to for that type.
          * `level`: For `orderbook` streams, this shows the grouping level.
      * **`ticket`**: The unique identifier from your request.

  * **Example Response**:
    This example shows a user subscribed to the ticker and orderbook for KRW-BTC and KRW-ETH.

    ```json
    {
      "method": "LIST_SUBSCRIPTIONS",
      "result": [
        {
          "type": "ticker",
          "codes": ["KRW-BTC", "KRW-ETH"]
        },
        {
          "type": "orderbook",
          "codes": ["KRW-BTC", "KRW-ETH"],
          "level": 0
        }
      ],
      "ticket": "your-unique-ticket"
    }
    ```